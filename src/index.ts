/*
 * @Description: 这是默认设置
 */
import ColorfulLogClass, { colorfulLog, materialColorfulLog } from './Colorful-log';
import * as styleValue from './styles';

export {
  styleValue,
  colorfulLog, 
  materialColorfulLog
};

export default ColorfulLogClass;